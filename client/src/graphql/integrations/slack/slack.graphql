query slackUsers {
	slackUsers {
    id
    fullName
    primaryEmail
    image
    thumbnail
  }
}

mutation syncSlackUser($data: InitialSyncSlackUserInput!) {
  initialSyncSlackUser(data: $data) {
    id
  }
}